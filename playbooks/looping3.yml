---
- name: Create multiple users
  hosts: all
  become: true
  tasks:
    - name: Create a users
      user:
        name: "{{ item }}"
        state: present
      with_items:
        - john
        - jane
        - joe

  